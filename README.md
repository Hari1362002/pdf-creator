# PDF Creator

Click anywhere on an A4 page, type in Tamil or English, pick a font and size, and download the page as a PDF or an image.

Plain HTML/CSS/JS — no build step. Open `index.html` or serve the folder statically.
